docker {
    enabled = true
    temp = 'auto'
    runOptions = '-u $(id -u):$(id -g)'
}

executor {
  name = 'local'
  cpus = 32
  memory = '128 GB'
}

params {
  phred       =       30
  krakenQC    =       null
}